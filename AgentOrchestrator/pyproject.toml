[project]
name = "ps-to-unity-agents"
version = "0.1.0"
description = "Evidence-first multi-agent orchestration for PS_To_Unity_v2"
requires-python = ">=3.11"
dependencies = [
  "openai-agents",
  "pillow>=10",
  "pydantic>=2",
]

[tool.uv]
package = false
